mascotas = ["taa", "ss", "ssaa", "qeq"]
primero, segundo = [1, 2]
# enumerate es para separar los elementos y convertir como un arreglo asociado
# podemos asignar que elementos de la lista podemos tomar de manera individual
for indice, mascota in enumerate(mascotas):
    print(indice, mascota)  # como un arreglo de datos
