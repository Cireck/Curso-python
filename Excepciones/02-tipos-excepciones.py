try:
    n1 = int(input("Ingresa primer numero: "))
    aadad
except ValueError as e:  # Exception es para tomar todas las exceptiones y alamacenar en la variable e
  # ValueError es para mostrar que tipo de valor el error
    print(type(e))
except NameError as e:
  # NameError es para mostrar que variable no fue declarada
    print(type(e))
